/* eslint-env mocha */

import { Meteor } from 'meteor/meteor';

if (Meteor.isServer) {
  describe('Task', () => {
    describe('methods', () => {
      it('can delete owned task', () => {        
      });
    });
  });
}